{
  "name": "@boramarca/core",
  "version": "0.1.0",
  "private": true,
  "description": "Núcleo compartilhado entre site e app. Agnóstico de framework: sem React, sem React Native, sem DOM.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check:agnostic": "node check-agnostic.mjs",
    "verify": "npm run typecheck && npm run test && npm run check:agnostic",
    "test": "node --import ./test-hook.mjs --test \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2",
    "typescript": "^5"
  },
  "peerDependencies": {
    "@supabase/supabase-js": "^2"
  }
}
